As @ops-engineer, address the @ui-ux-designer review findings for the settings demo UI fix.

Read these first:
- AGENTS.md
- spec/agents/README.md
- spec/agents/ops-engineer.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/README.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/ui-ux-findings.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/plan.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/progress.md

Task:
Address the @ui-ux-designer review findings from `ui-ux-findings.md` for the settings demo UI regression fix.

Implementation scope:
- **Finding 1 (Medium — required):** In `SamplesApp.applyTheme(...)`, replace `textPrimaryCss` with a contrast-on-accent color for the `-fx-text-fill` of `loginDemoButton` and `themeToggle`. Use `UiCommonThemeSupport.isDark(accent) ? Color.WHITE : Color.BLACK` to compute the correct high-contrast text color on accent-filled buttons. This fixes a WCAG AA contrast regression in light mode.
- **Finding 2 (Low — required):** Note that top bar buttons lack hover/focus pseudo-class states because inline `setStyle(...)` overrides CSS rules. Accept as a known demo-app limitation or, if straightforward, switch to style classes.
- **Finding 3 (Low — required):** In `SettingsPanel.buildPalette(...)`, replace the fully-qualified `javafx.scene.paint.Color.BLACK` with an imported `Color.BLACK` for code style consistency.

Priority:
P1 (follow-up to active P1 task)

Impacted modules:
- papiflyfx-docking-settings
- papiflyfx-docking-samples

Key invariants:
- Preserve all prior phase work — token injection, shared primitives, theme-aware navigation.
- Do not introduce new tokens or widen API/SPI contracts.
- Accent-background buttons must meet WCAG AA contrast (4.5:1) in both light and dark themes.

Acceptance criteria:
- `loginDemoButton` and `themeToggle` use a contrast-on-accent text fill color that is readable on both light and dark accent backgrounds.
- `SettingsPanel.buildPalette(...)` uses imported `Color.BLACK` instead of the fully-qualified reference.
- `papiflyfx-docking-settings` and `papiflyfx-docking-samples` compile cleanly and headless tests remain green.

Validation expectations:
- `./mvnw -pl papiflyfx-docking-settings,papiflyfx-docking-samples -am compile`
- `./mvnw -pl papiflyfx-docking-settings -Dtestfx.headless=true test`
- `./mvnw -pl papiflyfx-docking-samples -Dtestfx.headless=true test`

Instructions:
- Finding 1 is required before close. Findings 2 and 3 are optional improvements — apply them if the fix is clean and low-risk.
- Update `progress.md` with the review follow-up status.
- If implementation is appropriate, make the change and close with the repository handoff contract.

Return:
1. Changes made
2. Validation performed
3. Final handoff block
