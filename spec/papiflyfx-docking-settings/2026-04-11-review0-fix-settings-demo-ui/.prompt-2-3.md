As @ops-engineer, address the follow-up dark-mode UI findings for the settings demo UI fix.

Read these first:
- AGENTS.md
- spec/agents/README.md
- spec/agents/ops-engineer.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/README.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/plan.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/progress.md
- papiflyfx-docking-settings/README.md
 
## Task:
Address the remaining dark-mode usability issues for text/password fields, dropdowns, textareas, checkboxes, listboxes, and buttons in the settings panel used by the demo app.
 
## Implementation scope:
- **Finding 1 (Medium — required):** The dark-mode text/password input issue is still not fixed in the demo for the `Authentication`, `Security`, `GitHub`, `Editor`, `Hugo`, and `Profiles` categories. Make all settings text/password editors in those panes match the preference search field and shared field chrome. Reuse the existing shared field styling pattern (`pf-ui-compact-field` / `pf-ui-field`, or an equivalent existing shared field class) so background, border, prompt text, text fill, and focus ring align with the search input instead of the default JavaFX dark rendering.
- **Finding 2 (Medium — required):** Make dark-mode dropdowns match the same shared field treatment. In `SettingsToolbar`, `EnumSettingControl`, `AuthenticationCategory`, and any category-specific selectors, ensure the closed control, arrow-button area, text fill, prompt text, border, focus ring, and popup list cells use explicit token-driven styling instead of the default JavaFX dark rendering. Reuse existing shared field and popup primitives such as `pf-ui-compact-field`, `pf-ui-field`, and `pf-ui-popup-list` where practical rather than adding a parallel dropdown styling path.
- **Finding 3 (Medium — required):** Make dark-mode textareas align with the shared field treatment. In `ProfilesCategory` and any other multiline editors, apply shared token-driven background, border, prompt/text fill, selection, and focus styling to both the outer `TextArea` chrome and its inner content region so it visually matches the settings search/input family while preserving multiline sizing and spacing. Do not introduce hardcoded dark-only values.
- **Finding 4 (Medium — required):** Add the same token-driven dark-mode treatment for checkboxes. In `BooleanSettingControl`, `AuthenticationCategory`, `GitHubCategory`, `EditorCategory`, `HugoCategory`, `McpServersCategory`, and any similar panes, ensure checkbox box chrome, checkmark, label text, hover, focus, selected, and disabled states remain readable and consistent in dark mode instead of falling back to raw Modena styling.
- **Finding 5 (Medium — required):** Add the same token-driven dark-mode treatment for listboxes/list views. In `SettingsCategoryList`, `SecurityCategory`, `McpServersCategory`, `AuthenticationCategory`, and any similar list-driven panes, ensure list backgrounds, borders, cell text, hover, selected, and selected-but-unfocused states remain readable in dark mode. Reuse existing shared list styling such as `pf-ui-popup-list` or the existing token-driven list treatment rather than introducing hardcoded colors.
- **Finding 6 (Medium — required):** Make settings buttons use shared token-driven button chrome in dark mode. For raw `Button` instances in settings categories and toolbar areas, reuse existing shared button primitives/classes such as `UiPillButton`, `pf-ui-compact-action-button`, `pf-ui-compact-action-button-secondary`, or an equivalent existing shared button class so default, hover, focused, pressed, and disabled states stay readable and consistent instead of falling back to Modena dark defaults.
- Likely impacted classes include `SettingsToolbar`, `SettingsCategoryList`, `BooleanSettingControl`, `EnumSettingControl`, `ProfilesCategory`, `SecurityCategory`, `McpServersCategory`, `AuthenticationCategory`, `GitHubCategory`, `EditorCategory`, `HugoCategory`, `PathSettingControl`, `SecretSettingControl`, and `settings.css` / shared `ui-common.css` touchpoints. Reuse existing helpers and style classes where possible instead of introducing parallel styling paths.
 
## Priority:
P1 (follow-up to active P1 task)
 
## Impacted modules:
 - papiflyfx-docking-settings
 - papiflyfx-docking-login
 - papiflyfx-docking-github
 - papiflyfx-docking-code
 - papiflyfx-docking-hugo
 - papiflyfx-docking-samples
 - spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui
 
## Key invariants:
 - Preserve the token-driven light/dark theming work already completed for the settings panel.
 - Do not change settings runtime, persistence behavior, or API/SPI contracts.
 - Reuse existing `-pf-ui-*` tokens and shared `ui-common.css` primitives instead of adding hardcoded dark-mode values.
 - Keep textfield, dropdown, textarea, checkbox, listbox, and button sizing/spacing consistent with the shared compact field, list, and compact action treatment already used elsewhere in the settings UI.
 
## Acceptance criteria:
 - Dark-mode text/password inputs in the `Authentication`, `Security`, `GitHub`, `Editor`, `Hugo`, and `Profiles` categories use the same shared field background, border, prompt/text fill, and focus treatment as the preference search input.
 - Settings dropdowns remain readable and visually consistent in dark mode, including the closed control, arrow area, and popup list cells.
 - Dark-mode textareas use background and border treatment consistent with the shared settings field chrome while preserving multiline behavior.
 - Dark-mode checkboxes use explicit token-driven treatment for box chrome, label text, hover, focus, selected, and disabled states.
 - Dark-mode listboxes/list views use explicit token-driven treatment for background, border, hover, selected, and inactive-selected states.
 - Dark-mode settings buttons use explicit token-driven treatment for default, hover, focus, pressed, and disabled states instead of falling back to raw JavaFX dark styling.
 - Affected controls share styling through existing classes/tokens rather than per-control inline styling.
 - Affected modules compile cleanly and headless tests remain green.
 
## Validation expectations:
 - `./mvnw -pl papiflyfx-docking-settings,papiflyfx-docking-login,papiflyfx-docking-github,papiflyfx-docking-code,papiflyfx-docking-hugo,papiflyfx-docking-samples -am compile`
 - `./mvnw -pl papiflyfx-docking-settings,papiflyfx-docking-login,papiflyfx-docking-github,papiflyfx-docking-code,papiflyfx-docking-hugo,papiflyfx-docking-samples -am -Dtestfx.headless=true test`
 
## Instructions:
 - Treat all findings as required before close.
 - Prefer a CSS/style-class fix over per-instance inline styling.
 - Update `progress.md` with the dark-mode follow-up status.
 - If implementation is appropriate, make the change and close with the repository handoff contract.
 
## Return:
 1. Changes made
 2. Validation performed
 3. Final handoff block
