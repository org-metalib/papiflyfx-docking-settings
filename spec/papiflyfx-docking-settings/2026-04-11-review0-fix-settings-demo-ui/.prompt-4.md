As @ops-engineer, implement the accepted pull request review fixes for the settings demo UI follow-up.

Read these first:
- AGENTS.md
- spec/agents/README.md
- spec/agents/ops-engineer.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/README.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/review-pr-fixes.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/plan.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/progress.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/validation.md
- papiflyfx-docking-settings/README.md

## Task:
Address the 7 accepted review items captured in `review-pr-fixes.md` for PR #10.

## Implementation scope:
- **Item 1 (P1 - required):** In `papiflyfx-docking-settings/src/main/java/org/metalib/papifly/fx/settings/secret/EncryptedFileSecretStore.java`, keep backup recovery for malformed/corrupted content, but fail closed for unrecoverable decrypt/authentication failures. If both primary and backup secret files contain a valid envelope but cannot be decrypted or authenticated, throw `IllegalStateException` with the original cause chain and leave the on-disk files untouched. Replace the current empty-store fallback tests in `EncryptedFileSecretStoreTest` with coverage for recoverable malformed-content fallback vs unrecoverable decrypt/tamper failure, and add a regression showing a later write cannot silently erase encrypted state after failed decrypt.
- **Item 2 (P2 - required):** In `papiflyfx-docking-settings/src/main/java/org/metalib/papifly/fx/settings/internal/AtomicFileWriter.java`, catch `AtomicMoveNotSupportedException`, retry the rename with `REPLACE_EXISTING`, and guarantee temp-file cleanup on all failure paths. Extend `AtomicFileWriterTest` with deterministic coverage for the fallback path and leftover `*.tmp` cleanup.
- **Item 3 (P2 - required):** In `papiflyfx-docking-settings/src/main/java/org/metalib/papifly/fx/settings/categories/AppearanceCategory.java`, restore mode-aware fallback resolution for `appearance.background` and `appearance.border` after `binder.load(context)` in both `buildSettingsPane(...)` and `reset(...)`, and mirror that same fallback in `buildTheme()`. Add a regression proving light-mode defaults survive load/reset/apply when those two values are unset.
- **Item 4 (P2 - required):** In `papiflyfx-docking-settings/src/main/java/org/metalib/papifly/fx/settings/ui/SettingsPanel.java`, prevent scope-change re-entrancy while `showCategory(...)` normalizes supported scopes. Add a panel-local suppression guard so toolbar scope callbacks do not re-enter `onScopeChanged()` mid-refresh, and keep dirty-property binding strictly one-in/one-out. Add focused `SettingsPanelFxTest` coverage for category switches with different supported scopes.
- **Item 5 (P2 - required):** In `SettingsPanel.buildPalette(...)`, map `surfaceControl` to a real surface fill instead of `UiCommonThemeSupport.border(...)`. Use a surface color such as `UiCommonThemeSupport.background(resolved)` so `-pf-ui-surface-control` and related fills stop inheriting border chrome. Extend `SettingsPanelFxTest` if practical to assert the shared control background token resolves to a surface color rather than the border token.
- **Item 6 (P3 - required):** In `papiflyfx-docking-settings/src/main/java/org/metalib/papifly/fx/settings/ui/SettingsCategoryList.java`, remove the generic list chrome path (`SettingsUiStyles.applyList(this)`) so the category list is styled only by its dedicated `pf-settings-category-list` selector. Add a small regression proving the category list no longer carries `pf-settings-list`, or otherwise assert the dedicated selector wins.
- **Item 7 (P3 - required):** Fix the broken spec/doc link called out in `review-pr-fixes.md` and ensure the affected task documentation in `spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui` resolves correctly.

## Priority:
P1 (the encrypted-secret data-loss path is the highest-severity item)

## Impacted modules:
- papiflyfx-docking-settings
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui

## Required reviewers:
- @auth-specialist
- @qa-engineer
- @ui-ux-designer

## Key invariants:
- No secret-store path may rewrite an unrecoverable decrypt/auth failure into an empty persisted store.
- Preserve settings runtime ownership, JSON persistence compatibility, and existing session/restore behavior.
- Keep empty fallback behavior only for recoverable malformed/corrupted content, not for valid-but-undecryptable encrypted stores.
- Reuse the existing `-pf-ui-*` token vocabulary and dedicated settings selectors; do not introduce new styling systems or hardcoded palette values.
- Scope normalization in `SettingsPanel` must be deterministic and must not duplicate dirty listeners or clear pane caches mid-render.

## Acceptance criteria:
- `EncryptedFileSecretStore` throws on unrecoverable decrypt/auth failure after both primary and backup fail, without erasing existing files.
- `AtomicFileWriter` succeeds on filesystems without `ATOMIC_MOVE` support and does not leave orphaned temp files.
- `AppearanceCategory` preserves light-mode defaults for background/border when explicit overrides are unset.
- `SettingsPanel` no longer re-enters `showCategory(...)` through scope normalization and no longer maps `surfaceControl` to a border color.
- `SettingsCategoryList` styling is controlled only by its dedicated selector.
- The broken spec/doc link is fixed.
- Relevant regression tests cover the new secret-store, atomic-write, appearance, and settings-panel behavior.

## Validation expectations:
- `./mvnw -pl papiflyfx-docking-settings -am compile`
- `./mvnw -pl papiflyfx-docking-settings -am -Dtest=EncryptedFileSecretStoreTest,AtomicFileWriterTest,SettingsPanelFxTest -Dsurefire.failIfNoSpecifiedTests=false -Dtestfx.headless=true test`
- `./mvnw -pl papiflyfx-docking-settings -am -Dtestfx.headless=true test`

## Instructions:
- Treat all 7 accepted review items as required before close; the review-body summary comments are not actionable work.
- Follow the execution order from `review-pr-fixes.md` unless a dependency-safe variation is clearly better: secret store -> atomic writer -> settings panel re-entrancy and palette -> appearance defaults -> category-list CSS -> broken link.
- Update `progress.md` with the PR review remediation status.
- Update `validation.md` with the automated checks, any manual verification performed, and any residual risks.
- Coordinate final review with `@auth-specialist` on secret-store behavior, `@qa-engineer` on regression coverage, and `@ui-ux-designer` on palette/list styling before close.
- If implementation is appropriate, make the change and close with the repository handoff contract.

## Return:
1. Changes made
2. Validation performed
3. Remaining risks
4. Final handoff block
