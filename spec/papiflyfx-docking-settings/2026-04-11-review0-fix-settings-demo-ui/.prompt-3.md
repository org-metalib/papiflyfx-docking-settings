As @qa-engineer, validate this PapiflyFX Docking change for regression risk.

Read these first:
- AGENTS.md
- spec/agents/README.md
- spec/agents/playbook.md
- spec/agents/qa-engineer.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/README.md
- papiflyfx-docking-settings/README.md
- spec/papiflyfx-docking-samples/README.md

Change summary:
Fix the settings demo UI regression by injecting missing `-pf-ui-*` theme tokens into `SettingsPanel`, standardizing settings toolbar/search controls on shared UI primitives, and making `SamplesApp` fully theme-aware instead of dark-only.

Files/modules under review:
- `papiflyfx-docking-settings/src/main/java/org/metalib/papifly/fx/settings/ui/SettingsPanel.java`
- `papiflyfx-docking-settings/src/main/java/org/metalib/papifly/fx/settings/ui/SettingsToolbar.java`
- `papiflyfx-docking-settings/src/main/java/org/metalib/papifly/fx/settings/ui/SettingsSearchBar.java`
- `papiflyfx-docking-samples/src/main/java/org/metalib/papifly/fx/samples/SamplesApp.java`
- `papiflyfx-docking-settings`
- `papiflyfx-docking-samples`

Expected behavior:
- `SettingsPanel` injects theme and metric variables on its root and refreshes them when the active theme changes.
- `SettingsToolbar` and `SettingsSearchBar` load shared styles and use the shared primitives/style classes expected by `ui-common.css`.
- `SamplesApp` top bar, content area, navigation category rendering, and tree theme follow the active `Theme` without hardcoded dark-only colors.
- The affected modules compile cleanly and the relevant headless tests pass without new regressions.

Return in qa-findings.md:
1. Missing regression coverage
2. The narrowest relevant automated checks
3. Manual verification still needed
4. Residual risk if merged as-is
