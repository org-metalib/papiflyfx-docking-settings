As @ops-engineer, address the follow-up dark-mode UI findings for the settings demo UI fix.

Read these first:
- AGENTS.md
- spec/agents/README.md
- spec/agents/ops-engineer.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/README.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/plan.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/progress.md
- papiflyfx-docking-settings/README.md
 
## Task:
Address the remaining dark-mode usability issues in the settings panel used by the demo app.
 
## Implementation scope:
- **Finding 1 (Medium — required):** Improve inactive settings-group readability in dark mode. In `SettingsCategoryList` and `settings.css`, ensure the rendered category name remains clearly legible when the category list is not focused. Do not rely on default Modena list-cell text behavior when the visible text is rendered by a child `Label`; apply shared text tokens explicitly for default, hover, selected, and selected-but-unfocused states. Prefer the existing selection tokens such as `-pf-ui-surface-selected` / `-pf-ui-surface-selected-inactive` instead of introducing hardcoded dark-only colors.
- **Finding 2 (Medium — required):** Make dark-mode text/password inputs match the preference search field background and shared field chrome. Reuse the existing shared field styling pattern (`pf-ui-compact-field` or an equivalent existing shared field class) for settings editors and category-specific form inputs so their background, border, prompt text, text fill, and focus ring align with the search input instead of the default JavaFX dark rendering.
- Likely impacted classes include `SettingsCategoryList`, `SettingsSearchBar`, `StringSettingControl`, `NumberSettingControl`, `PathSettingControl`, `SecretSettingControl`, `KeyboardShortcutsCategory`, `McpServersCategory`, and `SecurityCategory`. Reuse existing helpers and style classes where possible instead of introducing parallel styling paths.
 
## Priority:
P1 (follow-up to active P1 task)
 
## Impacted modules:
 - papiflyfx-docking-settings
 - spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui
 
## Key invariants:
 - Preserve the token-driven light/dark theming work already completed for the settings panel.
 - Do not change settings runtime, persistence behavior, or API/SPI contracts.
 - Reuse existing `-pf-ui-*` tokens and shared `ui-common.css` primitives instead of adding hardcoded dark-mode values.
 - Keep field sizing and spacing consistent with the shared compact-field treatment already used by the search input.
 
## Acceptance criteria:
 - Settings category names remain readable in dark mode when the category list is not focused.
 - The inactive selected category state uses an explicit token-driven treatment instead of falling back to unreadable default text behavior.
 - Dark-mode settings text/password inputs use background and border treatment consistent with the preference search input.
 - Affected inputs share styling through existing classes/tokens rather than per-control inline styling.
 - `papiflyfx-docking-settings` compiles cleanly and headless tests remain green.
 
## Validation expectations:
 - `./mvnw -pl papiflyfx-docking-settings -am compile`
 - `./mvnw -pl papiflyfx-docking-settings -am -Dtestfx.headless=true test`
 
## Instructions:
 - Treat both findings as required before close.
 - Prefer a CSS/style-class fix over per-instance inline styling.
 - Update `progress.md` with the dark-mode follow-up status.
 - If implementation is appropriate, make the change and close with the repository handoff contract.
 
## Return:
 1. Changes made
 2. Validation performed
 3. Final handoff block
