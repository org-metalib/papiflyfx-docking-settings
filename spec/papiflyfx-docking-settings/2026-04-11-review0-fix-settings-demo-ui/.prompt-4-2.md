As @ops-engineer, address the blocking reviewer findings from the PR #10 settings demo UI remediation review.

Read these first:
- AGENTS.md
- spec/agents/README.md
- spec/agents/ops-engineer.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/README.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/review-pr-fixes.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/progress.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/validation.md
- papiflyfx-docking-settings/README.md

## Task:
Address the remaining blocker found in the multi-reviewer pass for the settings demo UI PR #10 remediation.

## Findings to address:
- **Finding 1 (P1 — required):** `papiflyfx-docking-settings/src/main/java/org/metalib/papifly/fx/settings/ui/SettingsCategoryList.java` still applies the generic `pf-settings-list` class. This violates accepted item 6 from `review-pr-fixes.md`, keeps category-list chrome under the generic selector path, and fails the new regression `SettingsPanelFxTest.categoryListUsesDedicatedSelectorOnly()`.
- **Finding 2 (P1 — required):** The validation record is stale. The reviewer reran the recorded commands and both the targeted settings suite and the full `papiflyfx-docking-settings` headless suite failed on the category-list selector regression. Update the implementation and then update `progress.md` / `validation.md` so they reflect the actual rerun results for this follow-up.

## Implementation scope:
- In `papiflyfx-docking-settings/src/main/java/org/metalib/papifly/fx/settings/ui/SettingsCategoryList.java`, remove the generic list-chrome path so the control is styled only by its dedicated `pf-settings-category-list` selector.
- Keep or tighten the focused regression in `papiflyfx-docking-settings/src/test/java/org/metalib/papifly/fx/settings/ui/SettingsPanelFxTest.java` so it still fails if `pf-settings-list` is reintroduced on `SettingsCategoryList`.
- Update `spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/progress.md` with the follow-up remediation status.
- Update `spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/validation.md` with the rerun compile/test results from this follow-up and keep manual visual/session verification called out if it is still pending.

## Priority:
P1

## Impacted modules:
- papiflyfx-docking-settings
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui

## Required reviewers:
- @qa-engineer
- @ui-ux-designer
- @spec-steward for the `progress.md` / `validation.md` updates

## Key invariants:
- `SettingsCategoryList` styling must be controlled only by the dedicated `pf-settings-category-list` selector path.
- Do not weaken or remove the regression that proves the generic `pf-settings-list` class is absent.
- Preserve the already accepted fixes in `EncryptedFileSecretStore`, `AtomicFileWriter`, `AppearanceCategory`, and `SettingsPanel`.
- Keep the shared `-pf-ui-*` token vocabulary and existing settings CSS structure intact.
- Validation records must match the actual rerun results in the workspace.

## Acceptance criteria:
- `SettingsCategoryList` no longer carries `pf-settings-list`.
- `SettingsPanelFxTest.categoryListUsesDedicatedSelectorOnly()` passes without relaxing its intent.
- `./mvnw -pl papiflyfx-docking-settings -am compile` passes.
- `./mvnw -pl papiflyfx-docking-settings -am -Dtest=EncryptedFileSecretStoreTest,AtomicFileWriterTest,SettingsPanelFxTest -Dsurefire.failIfNoSpecifiedTests=false -Dtestfx.headless=true test` passes.
- `./mvnw -pl papiflyfx-docking-settings -am -Dtestfx.headless=true test` passes.
- `progress.md` and `validation.md` accurately reflect this follow-up and any remaining manual verification.

## Validation expectations:
- `./mvnw -pl papiflyfx-docking-settings -am compile`
- `./mvnw -pl papiflyfx-docking-settings -am -Dtest=EncryptedFileSecretStoreTest,AtomicFileWriterTest,SettingsPanelFxTest -Dsurefire.failIfNoSpecifiedTests=false -Dtestfx.headless=true test`
- `./mvnw -pl papiflyfx-docking-settings -am -Dtestfx.headless=true test`

## Instructions:
- Treat both findings as required before close.
- Keep the fix narrowly scoped to the category-list selector collision and the stale validation/progress record.
- Do not re-open already accepted areas unless the rerun exposes a new blocker.
- If implementation is appropriate, make the change and close with the repository handoff contract.

## Return:
1. Changes made
2. Validation performed
3. Remaining risks
4. Final handoff block
