As the required reviewers (`@auth-specialist`, `@qa-engineer`, `@ui-ux-designer`), review the completed PR #10 settings demo UI follow-up remediation.

Read these first:
- AGENTS.md
- spec/agents/README.md
- spec/agents/auth-specialist.md
- spec/agents/qa-engineer.md
- spec/agents/ui-ux-designer.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/README.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/review-pr-fixes.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/progress.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/validation.md
- papiflyfx-docking-settings/README.md

## Review task:
Review the implemented fixes for the 7 accepted PR review items captured in `review-pr-fixes.md`. Use the final handoff below as the implementation summary and verify that the required security, regression, and UI expectations are satisfied before close.

## Final handoff block:
Lead Agent: `@ops-engineer`
Task Scope: PR #10 accepted review fixes for the settings demo UI follow-up, items 1-7
Impacted Modules: `papiflyfx-docking-settings`, `spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui`
Files Changed: secret store/runtime (`EncryptedFileSecretStore`, `AtomicFileWriter`), settings UI (`SettingsPanel`, `AppearanceCategory`, `SettingsCategoryList`), regressions (`EncryptedFileSecretStoreTest`, `AtomicFileWriterTest`, `SettingsPanelFxTest`), task docs (`README.md`, `progress.md`, `validation.md`, prompt references)
Key Invariants: unrecoverable decrypt/auth never rewrites to empty state; malformed backup fallback preserved; atomic writes leave no orphaned temp files; scope normalization is deterministic; `-pf-ui-*` token usage preserved; category list styling stays on dedicated selectors
Validation Performed: compile PASS; targeted regression suite PASS; full `papiflyfx-docking-settings` headless suite PASS
Open Risks / Follow-ups: manual visual/session verification still pending; reviewer confirmation still needed from security/QA/UI
Required Reviewer: `@auth-specialist`, `@qa-engineer`, `@ui-ux-designer`

## Files/modules under review:
- `papiflyfx-docking-settings/src/main/java/org/metalib/papifly/fx/settings/secret/EncryptedFileSecretStore.java`
- `papiflyfx-docking-settings/src/main/java/org/metalib/papifly/fx/settings/internal/AtomicFileWriter.java`
- `papiflyfx-docking-settings/src/main/java/org/metalib/papifly/fx/settings/categories/AppearanceCategory.java`
- `papiflyfx-docking-settings/src/main/java/org/metalib/papifly/fx/settings/ui/SettingsPanel.java`
- `papiflyfx-docking-settings/src/main/java/org/metalib/papifly/fx/settings/ui/SettingsCategoryList.java`
- `papiflyfx-docking-settings/src/test/java/org/metalib/papifly/fx/settings/secret/EncryptedFileSecretStoreTest.java`
- `papiflyfx-docking-settings/src/test/java/org/metalib/papifly/fx/settings/internal/AtomicFileWriterTest.java`
- `papiflyfx-docking-settings/src/test/java/org/metalib/papifly/fx/settings/ui/SettingsPanelFxTest.java`
- `spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/README.md`
- `spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/progress.md`
- `spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/validation.md`
- Prompt/doc reference fixes in this task pack

## Reviewer focus:
- `@auth-specialist`: verify the secret-store change fails closed for unrecoverable decrypt/authentication failures, preserves recoverable malformed-content fallback, preserves on-disk encrypted state after failed decrypt, and does not weaken secure-default behavior.
- `@qa-engineer`: verify the new regressions are narrow, deterministic, and sufficient for the reported failure modes, and confirm the recorded automated validation matches the scope of the fixes.
- `@ui-ux-designer`: verify `surfaceControl` now resolves to a surface fill instead of border chrome, category-list styling is controlled by the dedicated selector path only, and the appearance/settings behavior still aligns with the shared `-pf-ui-*` token vocabulary.

## Acceptance points to confirm:
- `EncryptedFileSecretStore` throws on unrecoverable decrypt/auth failure after both primary and backup fail, without erasing existing files.
- `AtomicFileWriter` succeeds on filesystems without `ATOMIC_MOVE` support and does not leave orphaned temp files.
- `AppearanceCategory` preserves light-mode defaults for background/border when explicit overrides are unset.
- `SettingsPanel` no longer re-enters `showCategory(...)` through scope normalization and no longer maps `surfaceControl` to a border color.
- `SettingsCategoryList` styling is controlled only by its dedicated selector.
- The broken spec/doc link is fixed.
- Relevant regression tests cover the new secret-store, atomic-write, appearance, and settings-panel behavior.

## Validation already recorded:
- `./mvnw -pl papiflyfx-docking-settings -am compile`
- `./mvnw -pl papiflyfx-docking-settings -am -Dtest=EncryptedFileSecretStoreTest,AtomicFileWriterTest,SettingsPanelFxTest -Dsurefire.failIfNoSpecifiedTests=false -Dtestfx.headless=true test`
- `./mvnw -pl papiflyfx-docking-settings -am -Dtestfx.headless=true test`

## Instructions:
- Treat this as a review pass, not a reimplementation pass, unless a blocking defect is found.
- Findings must come first and be ordered by severity with file references.
- If one review area has no findings, say that explicitly.
- Call out any remaining manual verification that should happen before merge.
- If the implementation is acceptable, state explicit sign-off status for each reviewer.

## Return:
1. `@auth-specialist` findings or sign-off
2. `@qa-engineer` findings or sign-off
3. `@ui-ux-designer` findings or sign-off
4. Cross-role blockers, if any
5. Remaining manual verification or residual risk
