As @spec-steward, intake this PapiflyFX Docking task.

Read these first:
- AGENTS.md
- spec/agents/README.md
- spec/agents/playbook.md
- spec/agents/spec-steward.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/README.md
- papiflyfx-docking-settings/README.md
- spec/papiflyfx-docking-samples/README.md

Task:
Fix the post-refactor settings demo UI regression across `papiflyfx-docking-settings` and `papiflyfx-docking-samples`.

Known issues:
- `settings.css` depends on `-pf-ui-*` token variables, but `SettingsPanel` never injects the token or metric variable blocks on its root, so text, backgrounds, and controls render incorrectly.
- `SettingsToolbar` and `SettingsSearchBar` are not fully aligned with shared `ui-common.css` primitives and style classes.
- `SamplesApp` still uses hardcoded dark-only colors and a static dark-only navigation theme, so the light/dark toggle produces broken or inconsistent visuals.
- The task should preserve settings runtime and persistence behavior while fixing theme/token wiring and demo UI chrome.

Return:
1. Priority (`P0`-`P3`)
2. Recommended lead agent
3. Required reviewers
4. Impacted modules and spec/docs paths
5. Key invariants and compatibility risks
6. Whether this should use the fast path or full spec-first workflow
7. Required artifacts (`research.md`, `plan.md`, `progress.md`, `validation.md`, README updates)
8. Suggested validation strategy
9. A short handoff block for the lead agent
