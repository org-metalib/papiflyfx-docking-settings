As @ops-engineer, lead this PapiflyFX Docking build/runtime task.

Read these first:
- AGENTS.md
- spec/agents/README.md
- spec/agents/playbook.md
- spec/agents/ops-engineer.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/README.md
- papiflyfx-docking-settings/README.md
- spec/papiflyfx-docking-samples/README.md

Task:
Fix the settings demo UI regression across `papiflyfx-docking-settings` and `papiflyfx-docking-samples`.

Implementation scope:
- In `SettingsPanel`, derive a `UiCommonPalette` from the active `Theme`, inject `UiCommonThemeSupport.themeVariables(...)` and `UiStyleSupport.metricVariables()` on the root, and refresh those variables when the theme changes.
- In `SettingsToolbar` and `SettingsSearchBar`, align the controls with shared UI primitives and classes from `papiflyfx-docking-api` (`UiCommonStyles.ensureLoaded(...)`, `UiPillButton`, `UiStatusSlot`, `pf-ui-field`, `pf-ui-compact-field`, `UiMetrics`).
- In `SamplesApp`, replace hardcoded dark-only colors in the top bar, content area, and navigation rendering with values derived from the active `Theme` via `UiCommonThemeSupport`, and replace the static navigation theme with a theme-aware builder.

Priority:
P1

Impacted modules:
- papiflyfx-docking-settings
- papiflyfx-docking-samples
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui

Required reviewers:
- @ui-ux-designer
- @qa-engineer

Key invariants:
- Preserve settings runtime ownership, persistence behavior, and existing settings data compatibility.
- Reuse the shared `-pf-ui-*` token vocabulary and existing UI primitives instead of introducing new styling systems or new hardcoded colors.
- Keep light/dark theme switching live and deterministic across settings and samples.
- Keep the change scoped to the owning modules; do not widen API/SPI contracts unless a clear blocker requires it.

Acceptance criteria:
- `SettingsPanel` injects theme and metric variables on its root and updates them when the active theme changes.
- `SettingsToolbar` and `SettingsSearchBar` use the shared UI primitives and style classes expected by `ui-common.css`.
- `SamplesApp` top bar, content area, navigation category rendering, and tree theme derive colors from the active `Theme`; the dark-only static navigation theme is removed.
- No task-specific hardcoded hex colors remain in the affected settings/sample UI surfaces.
- `papiflyfx-docking-settings` and `papiflyfx-docking-samples` compile cleanly, and the relevant headless tests remain green.

Validation expectations:
- `./mvnw -pl papiflyfx-docking-settings,papiflyfx-docking-samples -am compile`
- `./mvnw -pl papiflyfx-docking-settings,papiflyfx-docking-samples -am -Dtestfx.headless=true test`
- Manual verification that the light/dark toggle restyles the settings panel, sample top bar, content area, and navigation tree without stale dark-only colors.

Instructions:
- Keep dependency and plugin management centralized in the parent `pom.xml` where appropriate.
- Preserve settings persistence compatibility unless the plan explicitly changes the format.
- Update samples or smoke coverage when the task changes repository-level integration behavior.
- Coordinate with `@ui-ux-designer` on token usage, shared primitives, and theme ergonomics before close.
- If implementation is appropriate, make the change and close with the repository handoff contract.

Return:
1. Build/runtime approach
2. Compatibility or environment risks
3. Validation performed or planned
4. Final handoff block
