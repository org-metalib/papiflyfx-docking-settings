As @ops-engineer, address the @qa-engineer validation findings for the settings demo UI fix.

Read these first:
- AGENTS.md
- spec/agents/README.md
- spec/agents/ops-engineer.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/README.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/qa-findings.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/plan.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/progress.md
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui/validation.md

Task:
Address the @qa-engineer follow-up findings from `qa-findings.md` for the settings demo UI regression fix.

Implementation scope:
- **Finding 1a (Medium — required):** In `papiflyfx-docking-settings/src/test/java/org/metalib/papifly/fx/settings/ui/SettingsPanelFxTest.java`, add a theme-toggle regression test that constructs the panel under `Theme.dark()`, toggles `runtime.themeProperty().set(Theme.light())`, and verifies the panel re-themes without exception. If practical, assert on the panel root style/token output instead of only "does not throw".
- **Finding 1b (Low — required):** In `papiflyfx-docking-samples/src/test/java/org/metalib/papifly/fx/samples/SamplesSmokeTest.java`, add narrow smoke coverage for the `SamplesApp` shell so `SamplesApp.applyTheme()` and the contrast-on-accent button styling path are exercised under headless tests.
- **Finding 1c (Low — required):** Expand `SamplesSmokeTest.allSamplesLoadWithoutException()` or add an equivalent test path so the sample catalog is built under both `Theme.dark()` and `Theme.light()` rather than dark mode only.
- **Manual verification findings (required before close):** Run the sample app and verify the settings panel theme toggle, `SamplesApp` shell theme toggle, accent button text contrast, session restore round-trip, and navigation tree category rendering in both themes. Record the results in `validation.md`.

Priority:
P1 (follow-up to active P1 task)

Impacted modules:
- papiflyfx-docking-settings
- papiflyfx-docking-samples
- spec/papiflyfx-docking-settings/2026-04-11-review0-fix-settings-demo-ui

Key invariants:
- Preserve all prior phase work — token injection, shared primitives, theme-aware navigation, and contrast-on-accent button text.
- Keep settings runtime ownership, persistence behavior, and session format unchanged.
- Keep added regression coverage deterministic and as narrow as possible for headless TestFX/Monocle runs.
- Do not widen API/SPI contracts unless a testability blocker makes it unavoidable and the change is explicitly justified.

Acceptance criteria:
- `SettingsPanelFxTest` exercises a dark/light theme toggle path and fails if panel re-theming regresses.
- `SamplesSmokeTest` or equivalent covers both light-mode sample construction and a `SamplesApp` shell theme-toggle smoke path that reaches `applyTheme()`.
- Manual verification results are captured for the settings panel, sample shell, accent buttons, session restore flow, and navigation tree rendering in both themes.
- `papiflyfx-docking-settings` and `papiflyfx-docking-samples` compile cleanly and headless tests remain green.

Validation expectations:
- `./mvnw -pl papiflyfx-docking-settings,papiflyfx-docking-samples -am compile`
- `./mvnw -pl papiflyfx-docking-settings -am -Dtestfx.headless=true test`
- `./mvnw -pl papiflyfx-docking-samples -am -Dtestfx.headless=true test`
- `./mvnw javafx:run -pl papiflyfx-docking-samples`

Instructions:
- Treat Finding 1a as the minimum closure item; address Findings 1b and 1c in the same pass unless a concrete test-infrastructure blocker appears.
- Prefer focused assertions and minimal new test helpers over broad or brittle UI checks.
- Update `progress.md` with the QA follow-up implementation status.
- Update `validation.md` with the automated checks and manual verification outcomes from this follow-up.
- If implementation is appropriate, make the change and close with the repository handoff contract.

Return:
1. Changes made
2. Validation performed
3. Final handoff block
